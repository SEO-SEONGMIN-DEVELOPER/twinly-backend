[
    {
        "listener": "twinly-stage-alb HTTPS:443",
        "rules": [
            {
                "priority": 20,
                "conditions": {
                    "pathPattern": ["/internal/*"]
                },
                "action": "fixed-response 403 (text/plain, \"Forbidden\")",
                "description": "퍼블릭 ALB 로 들어온 /internal/* 는 출처와 무관하게 앱까지 가지 않고 ALB 에서 차단. AI 서버는 2026-09-03 부터 twinly-stage-internal-alb HTTP:8080 (기본 액션 forward -> twinly-stage-internal-api-tg) 을 경유하므로, 이전에 있던 NAT EIP 허용 규칙(priority 10)은 제거됐다."
            }
        ]
    },
    {
        "listener": "twinly-prod-alb HTTPS:443",
        "rules": [
            {
                "priority": 20,
                "conditions": {
                    "pathPattern": ["/internal/*"]
                },
                "action": "fixed-response 403 (text/plain, \"Forbidden\")",
                "description": "퍼블릭 ALB 로 들어온 /internal/* 는 출처와 무관하게 차단. AI 서버는 2026-09-03 부터 twinly-prod-internal-alb HTTP:8080 (기본 액션 forward -> twinly-prod-internal-api-tg) 을 경유하므로, 이전에 있던 NAT EIP 허용 규칙(priority 10)은 제거됐다."
            }
        ]
    }
]
