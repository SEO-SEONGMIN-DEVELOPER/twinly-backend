spring:
  config:
    import: optional:file:../.env[.properties]
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  file:
    name: logs/app.log

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

springwolf:
  enabled: true
