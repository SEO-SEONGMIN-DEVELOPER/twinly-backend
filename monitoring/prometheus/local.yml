global:
  scrape_interval: 15s
  external_labels:
    env: local

scrape_configs:
  - job_name: twinly-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - host.docker.internal:8081
        labels:
          node: local
